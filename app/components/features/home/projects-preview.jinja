{#import "@ui/card.jinja" as Card #}
{#import "@ui/tag.jinja" as Tag #}
{#import "@ui/section-link.jinja" as SectionLink #}
{#import "@ui/row.jinja" as Row #}
{#import "@ui/empty-state.jinja" as EmptyState #}
{#import "@ui/card-heading.jinja" as CardHeading #}
{#def featured=() #}

<section class="flex flex-col min-h-0">
    <a
        href="/projects"
        class="group block mb-3"
        data-analytics-event="click"
        data-analytics-id="home-projects-heading"
        data-analytics-label="projects"
        data-analytics-target="/projects"
    >
        <h2 class="text-lg font-semibold text-white flex items-center gap-2 group-hover:text-blue-400 transition-colors">
            Projects
        </h2>
    </a>

    <div class="space-y-3 flex-1 overflow-hidden">
        {% for project in featured %}
        <Card href={{ '/projects/' ~ project.slug }} padding="sm">
            <div class="flex flex-col gap-1">
                <CardHeading title={{ project.title }} date={{ project.date }} featured={{ project.featured }} title_class="text-sm text-white font-medium group-hover:text-blue-400 transition-colors" featured_class="text-green-500" />
                <p class="text-xs text-gray-500 line-clamp-1">{{ project.description }}</p>
                <Row wrap={{ true }} gap="1.5" class_name="mt-1">
                    {% for tech in (project.tech_stack if project.tech_stack else project.tags)[:3] %}
                    <Tag text={{ tech }} />
                    {% endfor %}
                </Row>
            </div>
        </Card>
        {% else %}
        <EmptyState message="No projects yet." />
        {% endfor %}
    </div>

    <div class="mt-3">
        <SectionLink href="/projects" label="View all projects" data-analytics-event="click" data-analytics-id="section-link-view-all-projects" data-analytics-label="View all projects" data-analytics-target="/projects" />
    </div>
</section>
