{#import "@layouts/public.jinja" as PublicLayout #}
{#import "@ui/button.jinja" as Button #}
{#import "@ui/center.jinja" as Center #}
{#def seo, current_path="" #}

<PublicLayout seo={{ seo }} current_path={{ current_path }}>
    <Center>
        <p class="text-sm text-gray-500 mb-3">Error</p>
        <h1 class="text-4xl sm:text-5xl font-bold text-white mb-3">404</h1>
        <p class="text-base text-gray-400 mb-6">The page you requested does not exist.</p>
        <Button href="/" variant="primary">
            Return Home
        </Button>
    </Center>
</PublicLayout>
