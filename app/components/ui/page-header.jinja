{#def title, subtitle="" #}

<header {{ attrs.render() }}>
    <h1 class="text-2xl sm:text-3xl font-bold text-white mb-2">{{ title }}</h1>
    {% if subtitle %}
    <p class="text-sm sm:text-base text-gray-400">{{ subtitle }}</p>
    {% endif %}
</header>
