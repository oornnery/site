{#def title, subtitle="", date_text="", chip="", size="base" #}

<div class="resume-entry-header">
    <div class="resume-entry-header__main">
        <p class="text-{{ size }} font-semibold text-foreground">{{ title }}</p>
        {% if chip %}
        <div class="resume-entry-subline">
            {% if subtitle %}
            <span class="text-foreground/70">{{ subtitle }}</span>
            {% endif %}
            <span class="resume-inline-chip">{{ chip }}</span>
        </div>
        {% elif subtitle %}
        <p class="text-sm leading-snug text-foreground/60">{{ subtitle }}</p>
        {% endif %}
    </div>
    {% if date_text %}
    <p class="resume-meta-label resume-entry-date">{{ date_text }}</p>
    {% endif %}
</div>
